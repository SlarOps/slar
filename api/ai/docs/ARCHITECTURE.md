# SLAR AI Architecture

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         main.py                              │
│                   (Single Entry Point)                       │
│  - FastAPI app initialization                               │
│  - Lifespan management                                       │
│  - Route registration                                        │
│  - CORS configuration                                        │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   config/    │    │    core/     │    │   routes/    │
│              │    │              │    │              │
│ - settings   │    │ - agent      │    │ - health     │
│              │    │ - session    │    │ - sessions   │
│              │    │ - tools      │    │ - runbook    │
│              │    │              │    │ - websocket  │
└──────────────┘    └──────────────┘    └──────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   models/    │    │    utils/    │    │  terminal/   │
│              │    │              │    │              │
│ - schemas    │    │ - helpers    │    │ - terminal   │
│              │    │ - indexers   │    │ - server     │
│              │    │ - slar_tools │    │              │
└──────────────┘    └──────────────┘    └──────────────┘
```

## 📦 Module Dependencies

```
main.py
  ├── config.get_settings()
  │     └── config.Settings
  │
  ├── core.SLARAgentManager
  │     ├── config.Settings
  │     ├── core.ToolManager
  │     └── utils.slar_tools
  │
  ├── core.SessionManager
  │     └── config.Settings
  │
  └── routes.*
        ├── models.schemas
        ├── core.*
        └── utils.*
```

## 🔄 Request Flow

### HTTP Request Flow
```
Client Request
     │
     ▼
┌─────────────┐
│  FastAPI    │ (main.py)
│  Middleware │
└─────────────┘
     │
     ▼
┌─────────────┐
│  Router     │ (routes/*)
└─────────────┘
     │
     ▼
┌─────────────┐
│ Validation  │ (models/schemas)
└─────────────┘
     │
     ▼
┌─────────────┐
│ Core Logic  │ (core/*)
└─────────────┘
     │
     ▼
┌─────────────┐
│  Response   │
└─────────────┘
```

### AI Agent Flow
```
User Input
     │
     ▼
┌──────────────────┐
│ SessionManager   │ (core/session.py)
│ - Get/Create     │
│ - Load State     │
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ SLARAgentManager │ (core/agent.py)
│ - Create Team    │
│ - Initialize MCP │
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ AutoGen Team     │
│ - Planning Agent │
│ - K8s Agent      │
│ - Code Executor  │
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ RAG Memory       │ (ChromaDB)
│ - Query KB       │
│ - Retrieve Docs  │
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ MCP Tools        │ (core/tools.py)
│ - Context7       │
│ - Custom Tools   │
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ AI Response      │
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ Session Save     │
└──────────────────┘
```

## 🗄️ Data Flow

### Document Indexing Flow
```
GitHub URL
     │
     ▼
┌──────────────────┐
│ GitHubRepoIndexer│ (utils/indexers.py)
│ - Clone/Download │
│ - Parse Files    │
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ Text Chunking    │
│ - Split into     │
│   chunks         │
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ ChromaDB         │
│ - Embed          │
│ - Store          │
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ Source Tracking  │ (utils/helpers.py)
│ - Save metadata  │
└──────────────────┘
```

### Session Persistence Flow
```
┌──────────────────┐
│ Active Session   │
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ Auto-Save Timer  │ (every 5 min)
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ SessionManager   │
│ - save_session() │
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ JSON Files       │
│ - session.json   │
│ - history.json   │
└──────────────────┘
```

## 🧩 Component Interaction

### Configuration Layer
```
┌───────────────────────────────────────┐
│           config/settings.py          │
│                                       │
│  Settings (Singleton)                 │
│  ├── Environment Variables           │
│  ├── Default Values                  │
│  └── Computed Properties             │
│                                       │
│  Used by: All modules                │
└───────────────────────────────────────┘
```

### Core Layer
```
┌───────────────────────────────────────┐
│        core/agent.py                  │
│  SLARAgentManager                     │
│  ├── create_sre_agent()              │
│  ├── create_k8s_agent()              │
│  ├── create_code_executor_agent()    │
│  ├── get_selector_group_chat()       │
│  └── initialize_mcp_tools()          │
└───────────────────────────────────────┘
         │
         ├─────────────────┐
         │                 │
         ▼                 ▼
┌─────────────┐    ┌─────────────┐
│core/session │    │core/tools   │
│             │    │             │
│SessionMgr   │    │ToolManager  │
│- save()     │    │- load_mcp() │
│- load()     │    │- workbench  │
└─────────────┘    └─────────────┘
```

### Utilities Layer
```
┌───────────────────────────────────────┐
│           utils/                      │
│                                       │
│  ┌─────────────┐  ┌──────────────┐  │
│  │  helpers    │  │  indexers    │  │
│  │             │  │              │  │
│  │- source_id  │  │- GitHub      │  │
│  │- detect     │  │- Content     │  │
│  └─────────────┘  └──────────────┘  │
│                                       │
│  ┌─────────────┐                     │
│  │ slar_tools  │                     │
│  │             │                     │
│  │- incidents  │                     │
│  └─────────────┘                     │
└───────────────────────────────────────┘
```

## 🔐 Security & Configuration

### Environment Variables
```
┌────────────────────────────────┐
│     Environment Variables       │
├────────────────────────────────┤
│ OPENAI_API_KEY    (Required)   │
│ OPENAI_MODEL      (Optional)   │
│ DATA_STORE        (Optional)   │
│ ENABLE_KUBERNETES (Optional)   │
│ ENABLE_CODE_EXEC  (Optional)   │
│ ... and more                    │
└────────────────────────────────┘
          │
          ▼
┌────────────────────────────────┐
│      config.Settings           │
│   (Centralized Access)         │
└────────────────────────────────┘
          │
          ▼
┌────────────────────────────────┐
│     All Application Modules    │
└────────────────────────────────┘
```

## 📊 State Management

### Session State
```
┌─────────────────────────────────────┐
│          Session State              │
├─────────────────────────────────────┤
│  session_id: str                    │
│  team: AutoGenTeam                  │
│  current_task: Optional[str]        │
│  is_streaming: bool                 │
│  last_activity: datetime            │
│  created_at: datetime               │
│  team_state: dict                   │
│  history: list[dict]                │
└─────────────────────────────────────┘
```

### Agent State
```
┌─────────────────────────────────────┐
│       SLARAgentManager State        │
├─────────────────────────────────────┤
│  settings: Settings                 │
│  rag_memory: ChromaDBVectorMemory   │
│  tool_manager: ToolManager          │
│  _model_client: OpenAIClient        │
│  _mcp_tools_cache: dict             │
│  _code_executor: DockerExecutor     │
└─────────────────────────────────────┘
```

## 🌐 API Endpoints

### Health Endpoints
```
GET /health
GET /health/memory
GET /health/stats
```

### Session Endpoints
```
POST   /sessions
GET    /sessions/{session_id}
DELETE /sessions/{session_id}
GET    /sessions
```

### Runbook Endpoints
```
POST /runbook/index
POST /runbook/retrieve
GET  /runbook/documents
GET  /runbook/documents/{doc_id}
```

### WebSocket
```
WS /ws/{session_id}
```

## 🔌 External Dependencies

```
┌─────────────────────────────────────┐
│     SLAR AI Application             │
└─────────────────────────────────────┘
     │
     ├──► OpenAI API (GPT-4o)
     │
     ├──► ChromaDB (Vector Store)
     │
     ├──► Docker (Code Execution)
     │
     ├──► MCP Servers
     │      └─► Context7
     │
     └──► External APIs
            └─► SLAR Backend (Incidents)
```

## 📈 Scalability Considerations

### Horizontal Scaling
- Stateless API design
- Session state in persistent storage
- External ChromaDB instance

### Vertical Scaling
- Async/await throughout
- Connection pooling
- Caching (MCP tools, settings)

### Performance Optimizations
- Lazy loading of MCP tools
- Settings singleton pattern
- Session auto-save batching
- ChromaDB query optimization

---

**Architecture Version**: 1.0.0
**Last Updated**: 2025-10-19
**Maintainer**: SLAR Team
